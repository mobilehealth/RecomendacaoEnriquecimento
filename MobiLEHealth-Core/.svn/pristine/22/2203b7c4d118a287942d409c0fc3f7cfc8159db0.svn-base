package mobilehealth.core;

import java.util.List;
import java.util.Map;

import mobilehealth.core.domain.Person;
/*import mobilehealth.sac.SACExternalService;
import mobilehealth.sac.SACExternalServiceImpl;
*/
public class CoreAPI {
	
	/**
	 * Gets interest degree of user for each domain registered in the system.
	 * The interest degree can vary 0-1, determining the relationship of the user with the domain knowledge.
	 * Abstracts the access of the semantic augmented component.
	 *  
	 * @param person
	 * @return Map list with domain (key) and interest degree.
	 */
	//TODO Jonathan: Definir como vai ser as interfaces
	public static List<Map<String, Double>> getInterestsByPerson(Person person) {
		/*SACExternalService SACservice = new SACExternalServiceImpl();
		return SACservice.getInterestsByPerson(person);*/
		return null;
	}
	
	
}
